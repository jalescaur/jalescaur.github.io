---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { getLangFromUrl, useTranslations } from '../i18n/utils';

interface Props { title?: string; description?: string; }

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const { title = t('site.title'), description = t('site.description') } = Astro.props;

const canonicalUrl = new URL(Astro.url.pathname, 'https://jalescaur.com');
const fullTitle = title === t('site.title') ? title : `${title} · ${t('site.title')}`;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <meta name="keywords" content="Jales Caur, International Relations, Relações Internacionais, Planetary Politics, Política Planetária, IR Theory, Data Science, OSINT, Political Consultant, BMJ Consultores, IREL UnB, Doutorado, PhD, Brasília, Lakatos, MSRP, Anthropocene" />
    <meta name="author" content="Jales Caur de Sousa" />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href={canonicalUrl} />

    <!-- Open Graph -->
    <meta property="og:type" content="profile" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:site_name" content="jales caur" />
    <meta property="og:locale" content={lang === 'pt' ? 'pt_BR' : 'en_US'} />
    <meta property="profile:first_name" content="Jales" />
    <meta property="profile:last_name" content="Caur" />

    <!-- Twitter/X Card -->
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />

    <!-- Schema.org JSON-LD — Person -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Person",
      "name": "Jales Caur de Sousa",
      "alternateName": "Jales Caur",
      "url": "https://jalescaur.com",
      "email": "contact@jalescaur.com",
      "jobTitle": "PhD Candidate in International Relations; Political and Data Consultant",
      "description": "Doctoral candidate at IREL/UnB researching planetary politics and IR metatheory. Political and Data Consultant at BMJ Consultores Associados leading the BMJ Digital product.",
      "affiliation": [
        {
          "@type": "Organization",
          "name": "University of Brasília — IREL",
          "url": "https://irel.unb.br"
        },
        {
          "@type": "Organization",
          "name": "BMJ Consultores Associados",
          "url": "https://bmj.com.br"
        }
      ],
      "alumniOf": [
        {
          "@type": "CollegeOrUniversity",
          "name": "University of Brasília",
          "url": "https://unb.br"
        },
        {
          "@type": "CollegeOrUniversity",
          "name": "Estácio"
        }
      ],
      "sameAs": [
        "https://www.linkedin.com/in/jalescaur/",
        "https://orcid.org/0000-0002-4057-9681",
        "http://lattes.cnpq.br/9989811261390202"
      ],
      "knowsAbout": [
        "International Relations Theory",
        "Planetary Politics",
        "Philosophy of Science",
        "Metatheory",
        "Data Science",
        "OSINT",
        "Discourse Analysis",
        "Political Risk",
        "Anthropocene"
      ],
      "address": {
        "@type": "PostalAddress",
        "addressLocality": "Brasília",
        "addressRegion": "DF",
        "addressCountry": "BR"
      }
    }
    </script>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="preload"
      as="style"
      href="https://fonts.googleapis.com/css2?family=Newsreader:ital,opsz,wght@0,6..72,400;0,6..72,500;1,6..72,400;1,6..72,500&family=Inter:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,500;1,600;1,700&family=Roboto+Condensed:ital,wght@0,400;0,500;0,600;0,700;1,400&family=Meddon&display=swap"
      onload="this.onload=null;this.rel='stylesheet'"
    />
    <noscript>
      <link
        rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Newsreader:ital,opsz,wght@0,6..72,400;0,6..72,500;1,6..72,400;1,6..72,500&family=Inter:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,500;1,600;1,700&family=Roboto+Condensed:ital,wght@0,400;0,500;0,600;0,700;1,400&family=Meddon&display=swap"
      />
    </noscript>
  </head>
  <body>
    <div class="page-frame">
      <Header />
      <main>
        <slot />
      </main>
      <Footer />
    </div>
  </body>
</html>

<style is:global>
  :root {
    --cream:        #FCFBF8;
    --cream-dark:   #F5F2EA;
    --cream-mid:    #E4E1D8;
    --ink:          #111111;
    --ink-mid:      #3A3A3A;
    --ink-light:    #6E6E6E;
    --ink-faint:    #A9A9A2;
    --accent:       #C60000;
    --accent-hover: #8F0000;
    --accent-dim:   rgba(198, 0, 0, 0.08);
    --rule:         #E4E1D8;
    --color-border: #E4E1D8;

    --font-serif:   'Newsreader', 'Georgia', serif;
    --font-display: 'Inter', system-ui, sans-serif;
    --font-sans:    'Inter', system-ui, sans-serif;
    --font-name:    'Meddon', cursive;
    --font-label:   'Roboto Condensed', system-ui, sans-serif;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html {
    background: var(--cream);
    color: var(--ink);
    font-family: var(--font-sans);
    font-size: 15px;
    line-height: 1.65;
    -webkit-font-smoothing: antialiased;
  }

  body { min-height: 100vh; background: var(--cream); }

  main {
    max-width: 1040px;
    margin: 0 auto;
    padding: 3rem 2rem 5rem;
  }

  a { color: var(--accent); text-decoration: none; }
  a:hover { color: var(--accent-hover); text-decoration: underline; text-decoration-color: var(--rule); }

  h1, h2, h3, h4 {
    font-family: var(--font-display);
    font-weight: 600;
    letter-spacing: -0.01em;
    line-height: 1.15;
    color: var(--ink);
  }

  h1 { font-size: clamp(2.5rem, 5vw, 3.75rem); font-style: normal; }
  h2 { font-size: clamp(1.3rem, 2.6vw, 1.7rem); margin-bottom: 1.25rem; }
  h3 { font-size: 1.05rem; font-weight: 500; }

  p { font-family: var(--font-sans); color: var(--ink-mid); line-height: 1.65; }

  .eyebrow {
    font-family: var(--font-label);
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--ink-faint);
    margin-bottom: 0.75rem;
    display: block;
  }

  hr.rule {
    border: none;
    border-top: 0.5px solid var(--rule);
    margin: 3rem 0;
  }

  .page-frame {
    min-height: 100vh;
    background: var(--cream);
  }

  @media (prefers-reduced-motion: reduce) {
    * { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }
  }
</style>