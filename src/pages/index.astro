---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
const recentPosts = (await getCollection('blog'))
  .filter(post => post.data.lang !== 'pt')
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3);
---

<BaseLayout>
  <div class="banner">
    <img src="/foto.png" alt="Jales Caur" fetchpriority="high" width="2400" height="960" />
    <div class="banner-fade"></div>
  </div>

  <section class="hero">
    <h1>Jales <em>Caur</em></h1>
    <p class="role-line">PhD Candidate — International Relations, University of Brasília (IREL/UnB) &middot; Political &amp; Data Consultant — BMJ Consultores Associados</p>
    <p class="tagline">Research, teaching and bridges between formal knowledge and the private sector.</p>
    <p class="bio">I am a doctoral candidate in International Relations at the University of Brasília (IREL/UnB), investigating planetary politics and the metatheoretical limits of IR as a discipline. I hold degrees in International Relations (UnB) and Data Science (Estácio), and I work as a Political and Data Consultant at BMJ Consultores Associados — leading the BMJ Digital product, which combines data science, OSINT, and discourse analysis.</p>
    <div class="hero-links">
      <a href="/research" class="link-arrow">Research →</a>
      <a href="/cv" class="link-arrow">Curriculum Vitae →</a>
      <a href="mailto:contact@jalescaur.com" class="link-arrow link-muted">contact@jalescaur.com</a>
    </div>
  </section>

  <section class="section">
    <div class="section-head">
      <h2>Selected work</h2>
      <a href="/research#publications" class="see-all">View all publications →</a>
    </div>
    <div class="work-list">
      <div class="work-item">
        <div class="work-meta">PhD thesis &middot; IREL/UnB &middot; in progress</div>
        <h3><a href="/research">Planetary Politics: A Beyond for International Relations?</a></h3>
        <p>Advised by Professor Cristina Yumie Aoki Inoue, PhD. Drawing on Lakatos&rsquo;s MSRP to examine whether planetary politics frameworks constitute a genuine metatheoretical shift in IR.</p>
      </div>
      <div class="work-item">
        <div class="work-meta">Consultant &middot; BMJ Consultores Associados &middot; 2024&ndash;present</div>
        <h3>BMJ Digital</h3>
        <p>Political analysis and digital risk monitoring — data science, OSINT, and discourse analysis applied to political and sectoral risk intelligence.</p>
      </div>
      <div class="work-item">
        <div class="work-meta">Book review &middot; International Affairs &middot; 2026</div>
        <h3><a href="/research#publications">Review of Ulf Hedetoft, <em>The Morality of Politics: States, Honour and War</em></a></h3>
        <p>Published in <em>International Affairs</em>, v. 102, p. 712–714.</p>
      </div>
    </div>
  </section>

  <hr class="rule" />

  <section class="section">
    <div class="section-head">
      <h2>From the blog</h2>
      <a href="/blog" class="see-all">All posts →</a>
    </div>
    <div class="blog-cards">
      {recentPosts.map(post => (
        <a href={`/blog/${post.id}`} class="blog-card">
          <div class="blog-card-date">{post.data.pubDate.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}</div>
          <div class="blog-card-title">{post.data.title}</div>
          {post.data.description && <div class="blog-card-desc">{post.data.description}</div>}
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

<style>
  .banner {
    position: relative;
    left: 50%;
    width: 100vw;
    margin-left: -50vw;
    margin-top: -3rem;
    height: 58vh;
    min-height: 420px;
    max-height: 620px;
    overflow: hidden;
    background: var(--cream-dark);
  }

  .banner img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: 50% 18%;
    filter: grayscale(1) contrast(1.15) brightness(1.03);
    display: block;
  }

  .banner-fade {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(17, 17, 17, 0) 55%, var(--cream) 100%);
  }

  .hero { padding: 3.5rem 0 4rem; max-width: 780px; }

  h1 { margin-bottom: 0.6rem; color: var(--ink); font-family: var(--font-name); font-weight: 400; font-size: clamp(2.75rem, 6vw, 4.25rem); }
  h1 em { font-style: normal; color: var(--accent); }

  .role-line {
    font-family: var(--font-label);
    font-size: 0.85rem;
    letter-spacing: 0.01em;
    color: var(--ink-light);
    margin-bottom: 1.5rem;
    max-width: 620px;
    line-height: 1.5;
  }

  .tagline {
    font-family: var(--font-display);
    font-style: italic;
    font-size: 1.2rem;
    color: var(--ink-mid);
    margin-bottom: 1.75rem;
    line-height: 1.5;
    display: block;
  }

  .bio { font-size: 1.02rem; margin-bottom: 2.25rem; max-width: 640px; }

  .hero-links { display: flex; gap: 0.5rem 1.75rem; flex-wrap: wrap; align-items: baseline; }

  .link-arrow {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.01em;
    color: var(--ink);
    text-decoration: none;
    border-bottom: 1px solid var(--ink);
    padding-bottom: 1px;
    transition: color 0.15s, border-color 0.15s;
  }
  .link-arrow:hover { color: var(--accent); border-color: var(--accent); text-decoration: none; }
  .link-arrow.link-muted { color: var(--ink-light); border-color: var(--rule); font-weight: 500; }
  .link-arrow.link-muted:hover { color: var(--accent); border-color: var(--accent); }

  .section { padding: 2.5rem 0; }
  .section-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 2rem;
  }
  .section-head h2 { margin-bottom: 0; }
  .see-all {
    font-family: var(--font-label);
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    color: var(--ink-light);
    white-space: nowrap;
  }
  .see-all:hover { color: var(--accent); text-decoration: none; }

  .work-list { display: flex; flex-direction: column; }
  .work-item {
    padding: 1.75rem 0;
    border-top: 1px solid var(--rule);
  }
  .work-item:last-child { padding-bottom: 0; }

  .work-meta {
    font-family: var(--font-label);
    font-size: 0.78rem;
    letter-spacing: 0.04em;
    color: var(--ink-light);
    text-transform: uppercase;
    margin-bottom: 0.5rem;
  }

  .work-item h3 { margin-bottom: 0.5rem; font-size: 1.25rem; }
  .work-item h3 a { color: var(--ink); }
  .work-item h3 a:hover { color: var(--accent); text-decoration: none; }
  .work-item p { font-size: 0.95rem; max-width: 680px; }

  .blog-cards { display: flex; flex-direction: column; gap: 0; }
  .blog-card {
    display: block;
    padding: 1.25rem 0;
    border-top: 1px solid var(--rule);
    text-decoration: none;
    transition: padding-left 0.2s ease;
  }
  .blog-card:last-child { border-bottom: 1px solid var(--rule); }
  .blog-card:hover { padding-left: 0.5rem; text-decoration: none; }
  .blog-card-date { font-family: var(--font-label); font-size: 0.72rem; letter-spacing: 0.1em; text-transform: uppercase; color: var(--ink-faint); margin-bottom: 0.3rem; }
  .blog-card-title { font-family: var(--font-display); font-size: 1.05rem; font-style: italic; color: var(--ink); margin-bottom: 0.2rem; transition: color 0.15s; }
  .blog-card:hover .blog-card-title { color: var(--accent); }
  .blog-card-desc { font-size: 0.85rem; color: var(--ink-light); }

  @media (max-width: 720px) {
    .banner { height: 44vh; min-height: 320px; }
    .section-head { flex-direction: column; align-items: flex-start; gap: 0.5rem; }
  }
</style>
